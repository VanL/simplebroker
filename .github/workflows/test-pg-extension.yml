name: Test Postgres Extension

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read

env:
  UV_VERSION: "0.12.0"

jobs:
  test-pg:
    env:
      UV_PYTHON: ${{ matrix.python-version }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.13", "3.14"]
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

    - name: Set up Python ${{ matrix.python-version }}
      id: matrix-python
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install uv
      uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9
      with:
        version: ${{ env.UV_VERSION }}
        enable-cache: true
        cache-dependency-glob: |
          pyproject.toml
          uv.lock
          extensions/simplebroker_pg/pyproject.toml
          extensions/simplebroker_pg/uv.lock

    - name: Install dependencies
      run: uv sync --frozen --extra dev --extra pg --python "${{ steps.matrix-python.outputs.python-path }}"

    - name: Verify matrix Python
      run: |
        uv run --frozen --no-sync python -c "import sys; expected = tuple(map(int, '${{ matrix.python-version }}'.split('.'))); assert sys.version_info[:2] == expected, (sys.version, expected)"

    - name: Run PG-backed test suites
      run: |
        uv run --frozen --no-sync ./bin/pytest-pg

  lint:
    runs-on: ubuntu-latest
    env:
      UV_PYTHON: "3.12"
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

    - name: Set up Python
      id: sync-python
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with:
        python-version: "3.12"

    - name: Install uv
      uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9
      with:
        version: ${{ env.UV_VERSION }}
        enable-cache: true
        cache-dependency-glob: |
          pyproject.toml
          uv.lock
          extensions/simplebroker_pg/pyproject.toml
          extensions/simplebroker_pg/uv.lock

    - name: Install dependencies
      run: uv sync --frozen --extra dev --extra pg --python "${{ steps.sync-python.outputs.python-path }}"

    - name: Run extension linting with ruff
      run: |
        uv run --frozen --no-sync ruff check extensions/simplebroker_pg/simplebroker_pg extensions/simplebroker_pg/tests

    - name: Check extension formatting with ruff
      run: |
        uv run --frozen --no-sync ruff format --check extensions/simplebroker_pg/simplebroker_pg extensions/simplebroker_pg/tests

    - name: Type check extension with mypy
      run: |
        mapfile -t pg_test_files < <(find extensions/simplebroker_pg/tests -type f -name '*.py' -not -path '*/__pycache__/*' | sort)
        uv run --frozen --no-sync mypy extensions/simplebroker_pg/simplebroker_pg "${pg_test_files[@]}" --config-file pyproject.toml
